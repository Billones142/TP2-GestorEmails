name: TestsConAccion

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  write-all

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: Billones142/jacoco-generateAndUploadResults@v1.0.1
        with:
          generate_image: true
